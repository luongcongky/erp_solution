# Database Configuration
# Choose ONE of the following options:

# Option 1: Use full connection string (RECOMMENDED for production)
DATABASE_URL=postgresql://username:password@host:port/database

# Option 2: Use Supabase connection string
# SUPABASE_DATABASE_URL=postgresql://postgres:your-password@db.xxxxx.supabase.co:5432/postgres

# Option 3: Use individual variables (for local development)
# POSTGRES_USER=postgres
# POSTGRES_PASSWORD=your-password
# POSTGRES_HOST=localhost
# POSTGRES_PORT=5432
# POSTGRES_DB=erp

# Node Environment
NODE_ENV=development

# For production on Vercel:
# 1. Go to Vercel Dashboard → Settings → Environment Variables
# 2. Add DATABASE_URL with your production database connection string
# 3. Make sure to use a strong password!
# 4. Enable SSL by setting NODE_ENV=production
